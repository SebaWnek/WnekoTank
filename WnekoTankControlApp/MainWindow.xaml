<Window x:Class="WnekoTankControlApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WnekoTankControlApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Slider x:Name="speedSlider" 
                Grid.Row="1" Grid.Column="2" 
                Grid.RowSpan="4"
                Orientation="Vertical"
                Maximum="100" 
                Minimum="-100" 
                Value="0" 
                TickFrequency="10" 
                IsSnapToTickEnabled="True" 
                ValueChanged="speedSlider_ValueChanged" 
                Thumb.DragCompleted="speedSlider_DragCompleted" HorizontalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="0" Text="{Binding ElementName=speedSlider, Path=Value}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <Slider x:Name="turnSlider" Grid.Row="1" 
                Grid.Column="0" Maximum="100" 
                Grid.RowSpan="4"
                Orientation="Vertical"
                IsDirectionReversed="True"
                Minimum="-100" 
                Value="0" 
                TickFrequency="5" 
                IsSnapToTickEnabled="True" 
                ValueChanged="turnSlider_ValueChanged" 
                Thumb.DragCompleted="turnSlider_DragCompleted" HorizontalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="2" Text="{Binding ElementName=turnSlider, Path=Value}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <Grid Grid.Row="2" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button x:Name="gear1btn" Grid.Column="0" Content="gear 1" Click="gear1btn_Click"/>
            <Button x:Name="gear2btn" Grid.Column="1" Content="gear 2" Click="gear2btn_Click"/>
        </Grid>
        <Button x:Name="stopBtn" Grid.Column="1" Grid.Row="3" Content="STOP" Click="stopBtn_Click" Margin="0,21,0,14"/>
        <Slider IsEnabled="False" 
                x:Name="angleSlider" 
                Grid.Row="1" Grid.Column="1" 
                Maximum="90" 
                Minimum="-90" 
                Value="0" 
                VerticalAlignment="Center" 
                TickFrequency="1" 
                IsSnapToTickEnabled="True" 
                Thumb.DragCompleted="angleSlider_DragCompleted"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ElementName=angleSlider, Path=Value}" VerticalAlignment="Top" HorizontalAlignment="Center"/>
        <ScrollViewer Grid.Row="4" Grid.Column="1" >
            <TextBlock x:Name="outputBox" />
        </ScrollViewer>
    </Grid>
</Window>
